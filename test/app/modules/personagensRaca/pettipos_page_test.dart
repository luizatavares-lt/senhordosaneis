import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

main() {
  testWidgets('PersonagensRacaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PettiposPage(title: 'PersonagensRaca')));
    //  final titleFinder = find.text('PersonagensRaca');
    //  expect(titleFinder, findsOneWidget);
  });
}
