import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

main() {
  testWidgets('PersonagensPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PetsPage(title: 'Personagens')));
    //  final titleFinder = find.text('Personagens');
    //  expect(titleFinder, findsOneWidget);
  });
}
